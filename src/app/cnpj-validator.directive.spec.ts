import { CnpjValidatorDirective } from './cnpj-validator.directive';

describe('CnpjValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CnpjValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
